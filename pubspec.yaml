name: dodam
description: "flutter project"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # 기본 제공 로컬라이제이션 추가
    sdk: flutter
  image_picker: ^1.1.2
  google_maps_flutter: ^2.10.0
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/
